new app.

1. typescript fe/be.
2. use asdf to manage node
3. can we deploy the entire project on vercel?
4. node with prisma for the BE
5. fe should be tanstack start client side only with tanstack query and shadecn
6. dark mode only / default
7. better-auth for login. we should only allow google provider (no other sign up), but require users to set a display name
